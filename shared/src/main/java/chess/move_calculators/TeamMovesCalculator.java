package chess.move_calculators;

import chess.ChessBoard;
import chess.ChessMove;
import chess.ChessPiece;
import chess.ChessPosition;

import java.util.Collection;

public class TeamMovesCalculator {
    // recursive function for this class, static because we just control it with param units
    public static Collection<ChessMove> Moves(
                                              ChessBoard board,
                                              Collection<ChessMove> moves) { // found out you can split these to see them better
        throw new RuntimeException("sorry");
    }
}
